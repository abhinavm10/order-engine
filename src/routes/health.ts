import { FastifyInstance } from 'fastify';

/**
 * Health check routes
 */
export const healthRoutes = async (app: FastifyInstance): Promise<void> => {
  // Already defined in app.ts - this is for future expansion
};
